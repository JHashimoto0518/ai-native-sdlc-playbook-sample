# レビュー

このリポジトリの差分をレビューするときの基準。人間のレビュアーも、Claude Code のセッションも、`.claude/agents/verifier.md` のサブエージェントも、同じこのファイルを読む。

参照する成果物: `intent/review-pass.md` → `spec/review-pass.md` → `plan/review-pass.md`

**これは助言的な制御である。** このファイルはレビューの質が揃う確率を上げるだけで、遵守を強制しない。決定的な裏付けは `make test`、`make lint`、`scripts/check-endpoints.sh`、`.claude/hooks/protect-paths.sh` の側にある。マージそのものを止める仕組み（ブランチ保護と CODEOWNERS）はまだ無い。`docs/phases.md` を参照。

## レビューのパス

3本を順に適用する。飛ばさない。

### パス1 — bugs

正しさを見る。

- 番号付き要件（`spec/*.md` の R1、R2 …）に対して、差分が実際にその挙動になっているか
- 境界条件、エラー経路、戻り値の型
- テストが通ることは前提とする。見るのは**検証自体が緩められていないか**である。アサーションの削除、テストの skip、期待値を実装に合わせる変更は、テストが緑でも所見になる

### パス2 — security

`.claude/skills/secure-api-review/SKILL.md` の5条項をそのまま適用する。レビュー用に基準を書き直さない（二重管理になり、いずれ食い違うため）。

外部公開エンドポイントに差分があるときは `./scripts/check-endpoints.sh` を実行し、**その出力をレビュー結果に引用する**。要約で置き換えない。

### パス3 — spec と plan への適合

このリポジトリに固有のパス。ここを飛ばすと、他のどこでもできるレビューになる。

- 差分のすべての変更が `intent/` `spec/` `plan/` のいずれかに辿れるか
- **辿れない変更は、それ自体が良い変更であっても Important 所見とする。** 成果物を先に更新させる
- 番号付き制約（`spec/*.md` の C1、C2 …）に違反していないか
- 成果物のほうが差分より古くなっていないか。実装を変えて spec を据え置いた差分は、この時点で見つける

## Important と nit

### Important

マージを止める。修正されるまで通さない。**件数に上限はない。**

- 挙動が仕様と違う
- 番号付き制約（C1/C2/C3 …）への違反
- `secure-api-review` の条項への違反
- 成果物に根拠のない変更（パス3）
- テストの検証を緩める変更
- 凍結パス `claims_api/legacy_v1/` への差分

### nit

マージを止めない。命名、コメントの言い回し、整形、構造の好み。

- 先頭に `nit:` を必ず付ける。付いていない所見は Important として読まれる
- **1レビューにつき3件まで。** 超える分は黙る

上限がある理由は、指摘が増えるほど Important のシグナルが薄まるからである。nit を4件書いたレビューは、3件書いて1件飲み込んだレビューより悪い。

判断に迷ったものは nit 側に置く。Important は「これを通すと壊れる」と言い切れるものに限る。

## 除外するパス

レビューしない。

- `.venv/`、`__pycache__/`、`.ruff_cache/`、`.pytest_cache/` — 生成物
- `.devcontainer/devcontainer-lock.json` — ロックファイル。ダイジェストを人が読んでも判断材料にならない

`claims_api/legacy_v1/` は**除外しない**。凍結パスなので差分は出ないはずだが、出た場合それ自体がパス3の Important 所見である。除外すると凍結が破れたことが見えなくなる。

## 出力の形

```
## 検証

（make test / make lint / scripts/check-endpoints.sh の出力をそのまま）

## Important

1. <file>:<line> — 所見。どのパスで、どの要件・制約に対して引っかかったか

## nit

1. nit: <file>:<line> — 所見
```

Important が無ければ「Important なし」と書く。所見を捻り出さない。
